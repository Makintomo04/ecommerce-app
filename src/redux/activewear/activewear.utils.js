export const filterShoes = (collection, filter) => {
  // console.log(collection[0].items);

  // collection[0].items = collection[0].items.filter(
  //   ({ gender }) => gender === filter
  // );
  // console.log(collection);
  return collection;
};
